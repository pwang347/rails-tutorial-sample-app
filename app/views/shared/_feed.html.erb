<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <!-- # Rails knows to call Micropost partial because each @feed_item has class Micropost (app/views/microposts/_micropost.html.erb) -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>